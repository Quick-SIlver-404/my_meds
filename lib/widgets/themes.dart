
import 'package:flutter/material.dart';

class MyTheme {
  static const titleStyle = TextStyle(
    fontSize: 25,
    fontWeight: FontWeight.bold,
  );

  static const noteStyle = TextStyle(
    fontSize: 18,
    color: Colors.grey
  );

  static const primaryColor = Color (0xFFFF494C) ;

  //Dark mode colors
  static const secondaryColor = Color(0xFF242339);
  static const bColor = Colors.purple;
  static const textColor = Colors.white;
  static const bodyTextColor = Color(0xFF9CA5BA);

  static const defaultPadding = 16.0;
  static const defaultBorderRadius = 14.0;

}